
git clone https://github.com/withr/RPiROV.git

git config remote.origin.url "https://github.com/withr/RPiROV.git"

git add --all; git commit -m "test"; git push


1. After burning Rasbian image into RPi's Micro SD card, enable the camera module and extent storage.
sudo raspi-config

2. update 
sudo apt-get update


3. 
